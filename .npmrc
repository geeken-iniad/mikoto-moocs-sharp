# Lockfile management
package-lock=true
prefer-frozen-lockfile=true

# Dependency strictness
auto-install-peers=false
strict-peer-dependencies=true

# Security
audit-level=high

# Supply chain attack mitigation
# Wait 7 days after package release before allowing installation
# This provides time for community review and vulnerability discovery
minimum-release-age=604800000
