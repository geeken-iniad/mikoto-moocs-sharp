{
  "name": "mikoto-moocs-sharp",
  "description": "MOOCsをより便利に - Monorepo",
  "license": "MIT",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:extension": "pnpm --filter @mikoto-moocs/extension dev",
    "dev:userscript": "pnpm --filter @mikoto-moocs/userscript dev",
    "dev:extension:firefox": "pnpm --filter @mikoto-moocs/extension dev:firefox",
    "build:extension": "pnpm --filter @mikoto-moocs/extension build",
    "build:userscript": "pnpm --filter @mikoto-moocs/userscript build",
    "build:all": "pnpm -r build",
    "compile": "pnpm -r compile"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
